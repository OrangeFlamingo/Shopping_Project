<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.mapper.UsersMapper">

    <select id="getUserInfo" parameterType="com.spring.models.UserVO" resultType="com.spring.models.UserVO">
        SELECT name AS userName FROM userinfo WHERE user_id = #{userId} AND passwd = #{userPassword}
    </select>    

    <insert id="insertUser" parameterType="com.spring.models.UserVO">
 		insert into 
 			userinfo
 				(user_id, passwd, name, phone_num, address ) 
 			values 
 				(
 					#{userId}
 					, #{userPassword}
 					, #{userName}
 					, #{userNum}
 					, #{address}
 				) 
	</insert>
    
    
</mapper>

